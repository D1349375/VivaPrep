# VivaPrep UI 設計規格與前端生成任務

> **用途：**交給另一個 session 生成／實作前端 prototype。  
> **狀態：**UI 規劃稿，不代表已開始實作。  
> **專案根目錄：**`VivaPrep/`  
> **視覺參考：**[面試室 UI 概念參考圖](references/面試室UI概念參考.png)

## 1. 產品情境與設計方向

VivaPrep 是高中生準備大學個人申請面試的模擬與復盤介面。主要體驗不是視訊會議，而是考生坐在面試位置、面對一整個面試室。畫面呈現長桌另一側的教授，使用者看不到自己的影像；面試中的字幕可由使用者自行選擇開啟或關閉。

### 視覺方向

- **房間寫實、人物微幅風格化：**保留自然光、長桌、資料夾、書架等正式面試室元素；教授是半寫實 3D 角色，臉部細節略簡化，不做照片級擬真，也不做卡通人物。
- **動作克制且有面試理由：**輪到的教授發問；其他人安靜聆聽或低頭查看資料，不安排誇張手勢、同步點頭或無意義的頻繁動作。
- **單一連續場景：**房間和教授共同構成整個主畫面，不做三個視訊 tile 或 Google Meet 格線。
- **中性校系場景：**參考圖目前意外生成了台大校名與標誌；成品需移除所有真實校名、校徽及未授權品牌，保持可套用不同大學的通用面試室。
- **寬螢幕優先：**第一版以桌機 16:9 面試畫面為主，確保教授臉部、字幕和控制列在常見筆電尺寸都看得清楚。

## 2. 核心畫面：面試室

### 構圖

- 相機位於考生坐姿的視線高度，像考生正坐在長桌近端。
- 三位教授坐在長桌遠端同一側，從左至右形成 A、B、C 三個視線區域；場景仍是一個完整房間，不以直線切割成三格。
- 教授臉部位於相近高度和清楚可辨識的位置，讓使用者可以自然地看向不同考官。A/B/C 標記若保留，應小而不搶眼，不能像正式面試中的名牌或大型 UI 標籤。
- 中間考官可以稍微靠近畫面中心，但不應大到變成單人視訊畫面；其餘兩位仍需可見。
- 桌面可遮擋下半身，但要保留足夠的手部、紙張和筆記本細節，呈現正在面試的情境。
- 不顯示考生自拍視窗。鏡頭在開始前的設備檢查頁可提供短暫預覽，進入正式模擬後即隱藏。

### 畫面 UI

| 位置 | 元件 | 規格 |
|---|---|---|
| 左上 | 狀態標籤 | 簡短顯示「模擬面試」或目前情境。 |
| 右上 | 校系情境 | 顯示學年度、招生管道、校系／組別；資料不完整時標示「一般模擬情境」。 |
| 教授區 | 發問者提示 | 以細緻光暈、座位邊框或小型狀態符號標出當前發問者；不要大幅縮放或移動鏡頭。 |
| 字幕區 | 即時字幕 | 由使用者自由開關；開啟時顯示一至兩行，位於畫面下方但不遮住教授臉和手部。預設狀態依使用者選擇保留。 |
| 底部 | 控制列 | 麥克風、攝影機狀態、字幕開關、結束面試。半透明、低高度，不遮住桌面互動。 |
| 右下或底部輸入區 | 文字模式 | 沒有麥克風時改為文字輸入與送出；仍保留教授房間場景。 |

正式模擬時不顯示即時分數、答案提示或教練文字。若要顯示時間，先作為練習設定選項，不在主畫面固定放置倒數計時。

## 3. 面試室以外的必要畫面

1. **情境設定：**選擇校系、招生組別、面試格式、面試時間、語音／文字模式，以及字幕偏好。校系資料缺漏時清楚標示，不暗示是官方流程。
2. **設備檢查：**確認麥克風、喇叭及攝影機；允許使用者先看一次攝影機預覽。說明進入模擬後不會顯示自拍畫面，以及攝影機訊號的使用方式。
3. **面試室：**整個房間是主舞台；操作不中斷對話，字幕可切換。
4. **復盤頁：**逐題檢視提問與回答、回答結構、引用到的備審內容、可觀察的語速／停頓／視線方向指標，以及下一輪可練習的題目。評語需能連回回答內容或時間點，不直接把視線行為解讀成性格或錄取結果。

## 4. 教授動作：前端可以先做，不必等待後端

房間渲染和教授的預設動作可以完全先在前端 prototype 裡展示。前端以本機 mock interview events 驅動畫面；之後後端再提供真正的提問、發問者和面試階段事件。**不要把動畫與 LLM API 綁在一起**，否則網路延遲或模型反應會讓角色卡住。

### 建議的角色狀態

| 狀態 | 動作表現 | 觸發方式 |
|---|---|---|
| `idle` 閒置 | 細微呼吸、低頻自然眨眼、維持坐姿 | 等候或非主要發問時 |
| `listening` 聆聽 | 視線朝向考生，偶爾輕微點頭或換一下坐姿 | 考生正在回答 |
| `reading_notes` 看資料 | 短暫低頭看備審、翻頁或寫一筆註記 | 考生回答一段時間後或輪替前 |
| `speaking` 發問 | 視線朝向考生，少量嘴部動作和一個短手勢 | 此教授正在說話 |
| `considering` 思考 | 短暫停頓、視線在資料與考生間移動 | 回答結束、準備追問 |
| `follow_up` 追問 | 小幅前傾，目光回到考生 | 根據答案觸發追問 |

### 隨機性設計：隨機但不能像抽籤亂動

使用者擔心照固定順序動作會讓模擬變得可預測，這個方向正確；但隨機性不應等於任意動作。建議用**受情境限制的機率排程**：

- 每場建立不同的 session seed，讓非關鍵小動作、短暫看資料時機和考官發問順序略有差異。
- 動作選擇須受到面試狀態限制：教授發問時朝向考生；考生回答時其他教授以聆聽為主，可有一位短暫記錄；不讓教授在考生回答時突然搶話或全員一起動作。
- 為眨眼、點頭、低頭看資料設最短間隔與冷卻時間；每位教授採不同動作節奏，避免像播放同一循環影片。
- 一次只允許一位教授處於主要 `speaking` 狀態；其他人的動作保持低幅度，發問者提示保持一致。
- 為測試保留固定 seed，方便重播同一情境；正式練習每次使用新的 seed。
- 第一版以少數預先定義的狀態轉場和動作片段組合，不追求自由生成的全身即興動作。

### 「動作隨機」和「題目不可預測」是兩件事

- 教授看資料、點頭、輪流發問等**動畫差異**，前端就能用 seed 和機率排程做出來，不需要後端。
- 根據學生答案改變追問、根據備審內容生成問題、調整教授角色與追問路徑，則需要面試協調／模型後端；在後端完成前，可用本機 mock question pool 和分支情境展示。
- 只隨機動畫並不能避免使用者背答案；真正避免「照固定題序對答案」要靠不同題目順序、答案承接式追問、同一能力的不同問法及學生自身備審內容。
- 動作與題目均須自然且符合評量目的。不能為了不可預測而讓考官隨機表現敵意、無故打斷或做不合情境的動作。

## 5. 前端與後端的介面界線

前端先建立一個清楚的事件介面；prototype 使用本機 mock event source，未來換成 WebSocket／後端事件時，房間元件與動畫播放不需重做。

概念事件欄位：

```text
sessionId       本場模擬識別碼
phase           setup / self_intro / question / follow_up / closing / ended
speakerId       interviewer-a / interviewer-b / interviewer-c
utterance       目前考官的文字或字幕內容
action          idle / listening / reading_notes / speaking / considering / follow_up
durationMs      動作建議持續時間
seed            本場動作排程種子（可選）
captionsEnabled 使用者字幕偏好
```

此為 UI 規格示例，不要求後端現在就實作該 API。前端把 `action` 映射到角色動畫；文字模式、麥克風狀態和字幕切換也以 UI 狀態管理。

### 建議開發順序

1. 先完成面試室的靜態構圖與所有 overlay，使用佔位人物確認構圖、可讀性和控制列。
2. 加入可重播的 mock interview scenario，驗證三位教授輪流發問、字幕切換、文字模式和結束流程。
3. 接入角色狀態轉場與有 seed 的小動作排程；先確保不重複、不搶話、不全員同動。
4. 接上真實 3D 模型與動畫片段，將模型放入固定的 `InterviewerAvatar` 畫面區域。
5. 後端完成後，將 mock event source 替換成 API／WebSocket，保持 UI 事件格式不變。

## 6. 交給另一個 session 的生成任務

請在 `VivaPrep` 專案現有技術架構內（先檢查現有前端框架與程式，不要另建不相容的一套）實作可操作的前端 prototype。**本次只處理前端呈現與 mock 互動，不串接真實 Gemini／後端 API，不產生或修改 3D 模型資產。**使用 mock interview events 演示一場面試，並可重設或切換 seed 查看動作變化。

優先完成：

1. 寬螢幕面試室場景與教授 A/B/C 的位置。
2. 字幕開關、麥克風／攝影機狀態、結束面試控制。
3. 模擬面試情境標籤和文字模式。
4. 三位教授 `idle/listening/reading_notes/speaking/follow_up` 狀態切換。
5. 可由前端 seed 產生差異、但受面試情境限制的教授小動作。
6. 設備檢查、情境設定與復盤頁面的基本導覽或可點擊 mock。

完成條件：

- 面試主畫面是一個完整面試室，不是視訊會議格線；看不到考生自拍。
- 字幕可隨時開關；關閉時字幕不佔位或遮擋畫面。
- 只會有一位教授主要發問，其餘角色動作克制且不同步。
- 同一 seed 可重現動作排程，不同 seed 可看到合理差異。
- 動作由 mock event/state 驅動，不在 UI 元件裡散落難以控制的 `Math.random()`。
- 使用中性校系背景，移除概念圖中誤生成的台大校名與校徽。
- 動作素材暫以 placeholder、簡單骨架／程序動畫或既有授權資源表示；不為了 MVP 延誤去打造高擬真人物。

## 7. 概念圖

以下圖像只作構圖、視角、字幕及操作列的參考；人物風格應依「房間寫實、人物微幅風格化」方向處理，並移除圖中誤生成的真實學校標誌。

![面試室 UI 概念參考](references/面試室UI概念參考.png)
